<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CloudStreamForms.Search">
    
    <!--
    <SearchBar x:Name="MainSearchBar" TextChanged="MainSearchBar_TextChanged">

    </SearchBar>
    -->
    <!--
    <ContentPage.Content>
        <ListView x:Name="MainSearchResultList" ItemTapped="MainSearchResultList_ItemTapped" ItemsSource="{Binding mySearchResultCollection}" HasUnevenRows="True" BackgroundColor="Green" SeparatorVisibility="None" VerticalOptions="FillAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding title}" Detail="{Binding extra}"></TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </ContentPage.Content>
    -->
</ContentPage>